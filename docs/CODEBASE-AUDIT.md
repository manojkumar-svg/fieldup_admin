# Codebase Audit

## Project
- Name: fieldup_admin (Field Up Admin)
- Framework: None yet — fresh project
- Language: TypeScript (to be set up)
- Package manager: npm (to be set up)

## Existing Stack
| Layer | Library | Version |
|-------|---------|---------|
| — | — | — |

No dependencies installed yet. This is a greenfield project.

## Directory Structure
```
fieldup_admin/
└── AGENTS.md
```

## Existing Patterns (pipeline must follow these)
- No existing patterns — this is a fresh project
- Will establish: absolute imports with `@/`, PascalCase components, Next.js App Router

## What Already Exists (DO NOT recreate)
- AGENTS.md — pipeline instructions

## What Needs to Be Built
- Full Next.js 14 App Router project setup
- PostgreSQL database with Prisma ORM
- Authentication system (admin login)
- Sports Courts CRUD (onboarding, management)
- Academies CRUD (onboarding, management)
- Trainers CRUD (onboarding, management)
- Admin dashboard with overview stats
- UI component library (Tailwind CSS)
- API routes for all entities
- Tests (unit, integration, E2E)
- Docker + CI/CD

## Conflicts / Risks
- None — fresh project, no conflicts possible
